# Optional — Google Drive library only (not required for free TTS)
GOOGLE_DRIVE_FOLDER_ID=17G-YzPxBMj41bpxCJoRhz4Rp7vGubrDZ
GOOGLE_API_KEY=

# Free TTS works with no keys (StreamElements + Translate fallbacks).
# Optional Google Cloud TTS upgrade later if you enable billing on GCP:
# GOOGLE_TTS_API_KEY=

# Optional durable store for uploads (shared across all listeners)
# Vercel → Storage → Blob
BLOB_READ_WRITE_TOKEN=
